'use client';
import {useState} from 'react';
import Link from 'next/link';
import {ArrowUpRight,Bookmark,BookOpen,Search,Star,Trash2,Users} from 'lucide-react';
import {useWorkspace} from '@/stores/workspace';
import {Empty,Entrance,PageHeading} from './ui';
import {number} from '@/lib/utils';
import {toast} from 'sonner';
export function Saved(){const {developers,repositories,removeSaved,clearSaved}=useWorkspace();const [tab,setTab]=useState<'developers'|'repositories'>('repositories');const [search,setSearch]=useState('');const [sort,setSort]=useState('recent');const matches=(text:string)=>text.toLowerCase().includes(search.toLowerCase());const people=developers.filter(d=>matches(`${d.login} ${d.name}`));const projects=repositories.filter(r=>matches(`${r.full_name} ${r.description}`));if(sort==='name'){people.sort((a,b)=>a.login.localeCompare(b.login));projects.sort((a,b)=>a.full_name.localeCompare(b.full_name));}else{people.reverse();projects.reverse();}return <Entrance><PageHeading eyebrow="KEEP THE GOOD FINDS CLOSE" title="Your collection" description="Interesting people. Inspiring projects. All in one place." actions={<span className="subtle-badge"><Bookmark size={13}/> {developers.length+repositories.length} saved</span>}/><div className="saved-toolbar"><div className="tabs"><button className={tab==='repositories'?'active':''} onClick={()=>setTab('repositories')}><BookOpen size={16}/>Repositories <span>{repositories.length}</span></button><button className={tab==='developers'?'active':''} onClick={()=>setTab('developers')}><Users size={16}/>Developers <span>{developers.length}</span></button></div><button className="text-button danger" disabled={!(tab==='developers'?developers:repositories).length} onClick={()=>{if(confirm(`Remove all saved ${tab}?`)){clearSaved(tab);toast.success('Collection cleared');}}}><Trash2 size={14}/>Clear all</button></div><div className="collection-filters"><label className="search-input"><Search size={17}/><input aria-label="Search saved items" placeholder="Search your collection…" value={search} onChange={e=>setSearch(e.target.value)}/></label><select aria-label="Sort saved items" value={sort} onChange={e=>setSort(e.target.value)}><option value="recent">Recently saved</option><option value="name">Name A–Z</option></select></div><p className="fine-print collection-note">Stored on this device. Saved metadata is a snapshot, not live GitHub data.</p>{tab==='developers'?(people.length?<div className="saved-grid">{people.map(d=><article className="card saved-developer" key={d.login}><img src={d.avatar_url} alt={d.login}/><div><Link href={`/developer/${d.login}`}><h2>{d.name||d.login}</h2><span className="mono muted">@{d.login}</span></Link><p>{d.bio||'A developer worth following.'}</p></div><button className="icon-button is-saved" onClick={()=>removeSaved('developers',d.login)} aria-label={`Remove ${d.login}`}><Bookmark size={17} fill="currentColor"/></button><Link className="text-link" href={`/developer/${d.login}`}>Explore profile <ArrowUpRight size={14}/></Link></article>)}</div>:<Empty title={search?'No matching developers':'Your people, in one place.'} description={search?'Try a different search.':'Save developers from their profile to follow their public work.'}><Link href="/analyzer" className="button primary">Find a developer</Link></Empty>):projects.length?<div className="repository-grid">{projects.map(r=><article className="repository-card saved-repo" key={r.id}><div className="repo-card-top"><BookOpen size={17}/><Link href={`/repository/${r.full_name}`} className="repo-title">{r.full_name}</Link><button className="icon-button is-saved" onClick={()=>removeSaved('repositories',r.id)} aria-label={`Remove ${r.name}`}><Bookmark size={17} fill="currentColor"/></button></div><p className="repo-description">{r.description||'No description provided.'}</p><div className="repo-metadata"><span>{r.language||'No language detected'}</span><span><Star size={14}/>{number(r.stargazers_count)} at save time</span></div></article>)}</div>:<Empty title={search?'No matching repositories':'Build your own little corner of open source.'} description={search?'Try a different search.':'Bookmark a repository and it will be waiting here next time.'}><Link href="/explore" className="button primary">Explore repositories</Link></Empty>}</Entrance>;}
